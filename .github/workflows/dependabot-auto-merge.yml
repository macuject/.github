name: Dependabot Auto-Merge

# Reusable workflow. Auto-merges minor/patch Dependabot PRs only after
# the caller's CI workflow (`inputs.workflow_name`, default `CI`) has
# completed with `conclusion=success` on the PR head commit.
#
# Triggered (in callers) by `workflow_run` on the CI workflow completing,
# and by `pull_request_review` on review submission, so the merge fires
# whichever signal arrives last: CI going green or the human approval.
#
# Performs the merge using ORG_TEAM_MEMBERS rather than GITHUB_TOKEN so
# that the resulting `pull_request: closed` event triggers downstream
# workflows (specifically update-jira-issue.yml, which sets fixVersion
# and transitions the linked Jira issue to Done). Merges performed by
# GITHUB_TOKEN do not create new workflow runs — see
# https://docs.github.com/en/actions/using-workflows/triggering-a-workflow#triggering-a-workflow-from-a-workflow
#
# A second job, `escalate-on-ci-failure`, handles the inverse case: when CI
# *fails* on a Dependabot PR that automation already auto-approved (Claude
# labelled it `claude-auto-merge-candidate` and posted an auto-approval review
# before CI completed), the merge job above is a no-op and the PR would sit
# orphaned — approved by a bot, red CI, no human looking. This job dismisses the
# stale approval and routes the PR to a human reviewer. It only runs when the
# caller passes `team_slug`, so consumers opt in by adding that input to their
# wrapper (no breakage for callers that haven't adopted it yet).

on:
  workflow_call:
    inputs:
      workflow_name:
        description: >-
          Name of the CI workflow whose latest run on the PR head SHA must
          have `conclusion=success` before a merge is allowed. The caller
          should also list this workflow under `on.workflow_run.workflows`.
          Defaults to `CI` to match `macuject/web`; `macuject/platform` and
          other consumers with a differently named workflow must override.
        type: string
        default: CI
      team_slug:
        description: >-
          GitHub team (slug under `macuject`) whose members are eligible
          reviewers when the `escalate-on-ci-failure` job routes an
          auto-approved Dependabot PR to a human after CI fails. Optional:
          when empty (the default) the escalation job is skipped entirely, so
          existing callers keep working until they opt in by setting this.
          e.g. `web`, `Platform`.
        type: string
        default: ""
      autofix_grace_seconds:
        description: >-
          Grace window (seconds) the `escalate-on-ci-failure` job waits for a
          newer commit to land on the PR head before escalating, letting an
          in-flight auto-fix workflow (e.g. a job that regenerates Sorbet RBIs
          and pushes them back) supersede the failing run. While the head SHA
          keeps advancing the failure is treated as transient, not terminal.
          Optional: defaults to 0, which means no wait and behaviour identical
          to before this input existed. Independent of this window, a failing
          run whose commit is no longer the PR head is always treated as stale
          and skipped.
        type: number
        default: 0
      autofix_paths:
        description: >-
          Path prefix the auto-fix workflow is allowed to touch (e.g.
          `sorbet/rbi/`). When commits land after the automated approval, the
          merge proceeds on that approval ONLY if every such commit was authored
          by `github-actions[bot]` and changes files solely under this prefix —
          i.e. it is the in-flight auto-fix, not unreviewed code. Anything else
          pushed after approval blocks the merge and routes the PR to a human.
          Optional: when empty (the default) ANY commit after the approval blocks
          the merge — the safe default for consumers without an auto-fix.
        type: string
        default: ""
      exclude_reviewers:
        description: >-
          Comma-separated team members never to assign as a human reviewer when a
          PR is escalated (e.g. bot accounts that belong to the team). Optional.
        type: string
        default: ""
    secrets:
      ORG_TEAM_MEMBERS:
        required: true

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    # Only run on CI success (workflow_run for a PR) or review submission.
    # A failed CI run or a non-PR workflow_run is a no-op.
    if: >-
      (github.event_name == 'workflow_run' &&
         github.event.workflow_run.conclusion == 'success' &&
         github.event.workflow_run.event == 'pull_request') ||
      github.event_name == 'pull_request_review'
    runs-on: ubuntu-latest
    steps:
      - name: Resolve PR number
        id: pr
        env:
          GH_TOKEN: ${{ secrets.ORG_TEAM_MEMBERS }}
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            NUM=$(jq -r '.workflow_run.pull_requests[0].number // empty' < "$GITHUB_EVENT_PATH")
          else
            NUM='${{ github.event.pull_request.number }}'
          fi
          if [ -z "$NUM" ] || [ "$NUM" = "null" ]; then
            echo "No PR associated with this event; nothing to do."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "number=$NUM" >> "$GITHUB_OUTPUT"

      - name: Gate on author, review, mergeable, CI Gate
        if: steps.pr.outputs.skip != 'true'
        id: gate
        env:
          GH_TOKEN: ${{ secrets.ORG_TEAM_MEMBERS }}
          PR_NUMBER: ${{ steps.pr.outputs.number }}
        run: |
          set -euo pipefail
          DATA=$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" \
            --json author,reviewDecision,mergeable,state,headRefOid)
          AUTHOR=$(jq -r '.author.login' <<<"$DATA")
          REVIEW=$(jq -r '.reviewDecision' <<<"$DATA")
          MERGEABLE=$(jq -r '.mergeable' <<<"$DATA")
          STATE=$(jq -r '.state' <<<"$DATA")
          SHA=$(jq -r '.headRefOid' <<<"$DATA")

          echo "author=$AUTHOR review=$REVIEW mergeable=$MERGEABLE state=$STATE sha=$SHA"

          if [ "$AUTHOR" != "app/dependabot" ]; then
            echo "Not a Dependabot PR; skipping."
            exit 0
          fi
          if [ "$STATE" != "OPEN" ]; then
            echo "PR not open (state=$STATE); skipping."
            exit 0
          fi

          # reviewDecision may lag behind the actual review submission by
          # several seconds due to GitHub API eventual consistency. When the
          # trigger is pull_request_review we know a review was just submitted,
          # so retry a few times before giving up. For workflow_run triggers
          # the review happened earlier, so a single check suffices.
          if [ "$REVIEW" != "APPROVED" ]; then
            if [ "${{ github.event_name }}" = "pull_request_review" ]; then
              echo "reviewDecision not yet APPROVED; retrying (eventual consistency)..."
              for i in 1 2 3; do
                sleep 5
                REVIEW=$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" \
                  --json reviewDecision --jq '.reviewDecision')
                echo "  attempt $i: reviewDecision=$REVIEW"
                if [ "$REVIEW" = "APPROVED" ]; then
                  break
                fi
              done
            fi
            if [ "$REVIEW" != "APPROVED" ]; then
              echo "PR not approved (reviewDecision=$REVIEW); skipping."
              exit 0
            fi
          fi

          # mergeable is computed asynchronously by GitHub and briefly returns
          # UNKNOWN after the head changes or CI completes (the merge state is
          # recomputed in the background). When the workflow_run trigger fires
          # right after CI finishes, we often hit this window. Retry on UNKNOWN
          # only — CONFLICTING is terminal, retrying won't help.
          if [ "$MERGEABLE" = "UNKNOWN" ]; then
            echo "mergeable=UNKNOWN; retrying (eventual consistency)..."
            for i in 1 2 3 4 5; do
              sleep 5
              MERGEABLE=$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" \
                --json mergeable --jq '.mergeable')
              echo "  attempt $i: mergeable=$MERGEABLE"
              if [ "$MERGEABLE" != "UNKNOWN" ]; then
                break
              fi
            done
          fi
          if [ "$MERGEABLE" != "MERGEABLE" ]; then
            echo "PR not mergeable (mergeable=$MERGEABLE); skipping."
            exit 0
          fi

          # Verify the latest run of the caller-specified CI workflow on
          # the PR head SHA finished `success`. Both triggers need this:
          # workflow_run guarantees one successful run existed, but the head
          # SHA may have moved since; pull_request_review has no CI info and
          # must look it up. Using the workflow's overall conclusion lets us
          # support consumers without a dedicated aggregator check job — the
          # conclusion is itself the aggregate (failure if any required job
          # failed, success if all succeeded or were skipped).
          WORKFLOW_NAME='${{ inputs.workflow_name }}'
          CONCL=$(gh api "repos/$GITHUB_REPOSITORY/actions/runs?head_sha=$SHA&event=pull_request" \
            --jq "[.workflow_runs[] | select(.name == \"$WORKFLOW_NAME\")] | sort_by(.run_started_at) | last | .conclusion // \"missing\"")
          if [ "$CONCL" != "success" ]; then
            echo "Workflow '$WORKFLOW_NAME' not success on $SHA (conclusion=$CONCL); skipping."
            exit 0
          fi

          echo "ok=true" >> "$GITHUB_OUTPUT"
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"

      - name: Determine update type from Dependabot commit metadata
        if: steps.gate.outputs.ok == 'true'
        id: metadata
        env:
          GH_TOKEN: ${{ secrets.ORG_TEAM_MEMBERS }}
          PR_NUMBER: ${{ steps.pr.outputs.number }}
        run: |
          set -euo pipefail
          # Dependabot commits embed metadata in the commit message body:
          #   update-type: version-update:semver-{patch,minor,major}
          # Grouped PRs contain multiple entries; the effective type is the
          # highest severity present (major > minor > patch).
          MSGS=$(gh api "repos/$GITHUB_REPOSITORY/pulls/$PR_NUMBER/commits" --paginate \
            --jq '.[].commit.message' | tr -d '\r')
          TYPES=$(grep -oE 'update-type: version-update:semver-(patch|minor|major)' <<<"$MSGS" \
            | awk -F'semver-' '{print $2}' | sort -u || true)

          # Dependabot omits the `update-type:` metadata line for *indirect*
          # (transitive) dependency bumps — the commit body only carries
          # `dependency-name`, `dependency-version`, and `dependency-type:
          # indirect` (see macuject/web#1835). Derive the classification from
          # the `Bumps [name](url) from X.Y.Z to A.B.C.` lines Dependabot
          # always emits, and union with any signal already extracted so
          # grouped PRs that mix direct and indirect entries pick the highest
          # severity across both. Pre-release/non-numeric version components
          # are skipped (rare for indirect deps; falls through to `unknown`).
          PAIRS=$(grep -oE 'Bumps .+ from [0-9]+\.[0-9]+\.[0-9]+ to [0-9]+\.[0-9]+\.[0-9]+' <<<"$MSGS" || true)
          while IFS= read -r line; do
            [ -z "$line" ] && continue
            OLD=$(awk '{for(i=1;i<=NF;i++) if($i=="from"){print $(i+1); exit}}' <<<"$line")
            NEW=$(awk '{for(i=1;i<=NF;i++) if($i=="to"){print $(i+1); exit}}' <<<"$line")
            [[ "$OLD" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ && "$NEW" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]] || continue
            IFS=. read -r OM OI OP <<<"$OLD"
            IFS=. read -r NM NI NP <<<"$NEW"
            if [ "$NM" -gt "$OM" ]; then
              TYPES="$TYPES"$'\n'major
            elif [ "$NM" = "$OM" ] && [ "$NI" -gt "$OI" ]; then
              TYPES="$TYPES"$'\n'minor
            elif [ "$NM" = "$OM" ] && [ "$NI" = "$OI" ] && [ "$NP" -gt "$OP" ]; then
              TYPES="$TYPES"$'\n'patch
            fi
          done <<< "$PAIRS"
          TYPES=$(printf '%s\n' "$TYPES" | sort -u | grep -v '^$' || true)
          echo "Detected update types: $TYPES"
          if grep -qx major <<<"$TYPES"; then
            TYPE=major
          elif grep -qx minor <<<"$TYPES"; then
            TYPE=minor
          elif grep -qx patch <<<"$TYPES"; then
            TYPE=patch
          else
            TYPE=unknown
          fi
          echo "Effective update-type: $TYPE"
          echo "update-type=$TYPE" >> "$GITHUB_OUTPUT"

      - name: Verify nothing unreviewed landed after the approval
        # An approval persists across pushes (`require_last_push_approval: false`
        # on the ruleset), so a commit pushed after the automated approval would
        # otherwise merge unreviewed. Allow exactly one exception: the in-flight
        # auto-fix (commits authored by github-actions[bot] touching only
        # `autofix_paths`). Anything else after the approval blocks the merge.
        if: >-
          steps.gate.outputs.ok == 'true' &&
          (steps.metadata.outputs.update-type == 'minor' ||
           steps.metadata.outputs.update-type == 'patch')
        id: postapproval
        env:
          GH_TOKEN: ${{ secrets.ORG_TEAM_MEMBERS }}
          PR_NUMBER: ${{ steps.pr.outputs.number }}
          HEAD_SHA: ${{ steps.gate.outputs.sha }}
          AUTOFIX_PATHS: ${{ inputs.autofix_paths }}
        run: |
          set -euo pipefail

          # The commit the standing approval was submitted against. With no
          # APPROVED review the gate would not have passed, so this is set.
          # `--paginate` runs the jq filter per page, so a cross-page reduction
          # like `last` would yield one value per page. Emit each APPROVED
          # commit_id (oldest to newest) and take the final line — the most
          # recent approval.
          APPROVED_SHA=$(gh api "repos/$GITHUB_REPOSITORY/pulls/$PR_NUMBER/reviews" --paginate \
            --jq '.[] | select(.state == "APPROVED") | .commit_id' | tail -n1)
          if [ -z "$APPROVED_SHA" ]; then
            echo "Could not resolve the approved commit; blocking merge as a precaution."
            echo "block=true" >> "$GITHUB_OUTPUT"; exit 0
          fi
          if [ "$APPROVED_SHA" = "$HEAD_SHA" ]; then
            echo "Head $HEAD_SHA is the approved commit; nothing landed after approval."
            echo "block=false" >> "$GITHUB_OUTPUT"; exit 0
          fi

          # Everything between the approved commit and the head must be the
          # auto-fix: authored by github-actions[bot] and confined to AUTOFIX_PATHS.
          # Use the GitHub-native author identity (`author.login`), not the git
          # `commit.author.name` — the latter is set by the committing workflow's
          # config and is spoofable, so a consumer whose auto-fix sets a different
          # name would silently fail closed (every post-approval commit blocked).
          CMP=$(gh api "repos/$GITHUB_REPOSITORY/compare/$APPROVED_SHA...$HEAD_SHA")
          NON_BOT=$(jq -r '[.commits[] | select((.author.login // "") != "github-actions[bot]")] | length' <<<"$CMP")
          # `files` is omitted/truncated for very large compares; absence ⇒ cannot
          # confirm the diff is confined, so treat as out-of-scope (block).
          if [ -z "$AUTOFIX_PATHS" ]; then
            OUTSIDE=1
          else
            OUTSIDE=$(jq -r --arg p "$AUTOFIX_PATHS" \
              '[(.files // [{}])[] | select((.filename // "x") | startswith($p) | not)] | length' <<<"$CMP")
          fi

          if [ "$NON_BOT" -eq 0 ] && [ "$OUTSIDE" -eq 0 ]; then
            echo "Post-approval commits are auto-fix-only ($APPROVED_SHA...$HEAD_SHA); merge may proceed on the existing approval."
            echo "block=false" >> "$GITHUB_OUTPUT"
          else
            echo "Commits landed after approval that are NOT the auto-fix (non-bot=$NON_BOT, outside-paths=$OUTSIDE); blocking merge."
            echo "block=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Route post-approval drift to human review
        if: steps.postapproval.outputs.block == 'true'
        uses: macuject/.github/.github/actions/escalate-dependabot-pr@main
        with:
          pr_number: ${{ steps.pr.outputs.number }}
          repo: ${{ github.repository }}
          team_slug: ${{ inputs.team_slug }}
          org_token: ${{ secrets.ORG_TEAM_MEMBERS }}
          dismiss_approval: "true"
          exclude_members: ${{ inputs.exclude_reviewers }}
          comment_body: >-
            ⚠️ Auto-merge cancelled: commits were pushed after this Dependabot PR
            was automatically approved that are not the expected auto-fix. The
            automated approval has been dismissed and the PR routed to a human
            reviewer. Please review the new commits before merging.

      - name: Merge minor/patch
        # Require an explicit `block=false`: if the post-approval check errored
        # (empty output), do NOT merge — fail safe rather than merge unverified.
        if: >-
          (steps.metadata.outputs.update-type == 'minor' ||
           steps.metadata.outputs.update-type == 'patch') &&
          steps.postapproval.outputs.block == 'false'
        env:
          GH_TOKEN: ${{ secrets.ORG_TEAM_MEMBERS }}
          PR_NUMBER: ${{ steps.pr.outputs.number }}
        run: gh pr merge --merge --repo "$GITHUB_REPOSITORY" "$PR_NUMBER"

  escalate-on-ci-failure:
    # Inverse of the merge job: when the caller's CI workflow completes with
    # `conclusion=failure` on a Dependabot PR that automation already
    # auto-approved, dismiss the stale approval and route the PR to a human.
    # Gated on `team_slug` so callers opt in (see header comment). Skipped for
    # CI successes and non-PR runs (the merge job handles successes).
    if: >-
      inputs.team_slug != '' &&
      github.event_name == 'workflow_run' &&
      github.event.workflow_run.conclusion == 'failure' &&
      github.event.workflow_run.event == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Resolve PR number
        id: pr
        run: |
          set -euo pipefail
          NUM=$(jq -r '.workflow_run.pull_requests[0].number // empty' < "$GITHUB_EVENT_PATH")
          if [ -z "$NUM" ] || [ "$NUM" = "null" ]; then
            echo "No PR associated with this event; nothing to do."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "number=$NUM" >> "$GITHUB_OUTPUT"

      - name: Gate — auto-approved Dependabot PR
        if: steps.pr.outputs.skip != 'true'
        id: gate
        env:
          GH_TOKEN: ${{ secrets.ORG_TEAM_MEMBERS }}
          PR_NUMBER: ${{ steps.pr.outputs.number }}
        run: |
          set -euo pipefail
          DATA=$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json author,state,labels)
          AUTHOR=$(jq -r '.author.login' <<<"$DATA")
          STATE=$(jq -r '.state' <<<"$DATA")
          LABELS=$(jq -c '[.labels[].name]' <<<"$DATA")

          # Only act on open Dependabot PRs that automation auto-approved (Claude
          # applied the candidate label). PRs without it were never auto-approved,
          # so there is no stale approval for this path to dismiss. Idempotency —
          # escalating once, not on every red run — is handled by the shared action
          # via its sentinel label, so it is not re-checked here.
          if [ "$AUTHOR" != "app/dependabot" ]; then
            echo "Not a Dependabot PR (author=$AUTHOR); skipping."; exit 0
          fi
          if [ "$STATE" != "OPEN" ]; then
            echo "PR not open (state=$STATE); skipping."; exit 0
          fi
          if ! jq -e 'index("claude-auto-merge-candidate")' <<<"$LABELS" >/dev/null; then
            echo "PR not an auto-merge candidate (automation never took ownership); skipping."
            exit 0
          fi
          echo "proceed=true" >> "$GITHUB_OUTPUT"

      # CI failed on an auto-approved Dependabot PR: dismiss the stale approval and
      # route to a human. The shared action handles the stale-run / auto-fix grace
      # guard, approval dismissal, reviewer assignment (honouring exclude_reviewers),
      # the comment, and once-only escalation via the sentinel label.
      - name: Escalate to human reviewer
        if: steps.gate.outputs.proceed == 'true'
        uses: macuject/.github/.github/actions/escalate-dependabot-pr@main
        with:
          pr_number: ${{ steps.pr.outputs.number }}
          repo: ${{ github.repository }}
          team_slug: ${{ inputs.team_slug }}
          org_token: ${{ secrets.ORG_TEAM_MEMBERS }}
          failed_sha: ${{ github.event.workflow_run.head_sha }}
          grace_seconds: ${{ inputs.autofix_grace_seconds }}
          dismiss_approval: "true"
          sentinel_label: auto-merge-ci-failed
          exclude_members: ${{ inputs.exclude_reviewers }}
          comment_body: >-
            ⚠️ Auto-merge cancelled: CI failed after this Dependabot PR was
            auto-approved. The automated approval has been dismissed and the PR
            routed to a human reviewer. Please review the CI failure and the
            changes before merging.

  escalate-needs-human:
    # The non-auto-approved counterpart to `escalate-on-ci-failure`. When the caller's
    # review step flags a Dependabot PR for a human (label `needs-human-review`, or
    # `claude-review-unavailable` when the review could not run) instead of auto-approving
    # it, the summons must still wait until the head is stable, so an in-flight auto-fix
    # (e.g. the RBI sync) isn't mistaken for a dead PR and the comment isn't buried under
    # the auto-fix's own updates. Driven by CI completion (any conclusion). Gated on
    # `team_slug` so callers opt in, and escalates only when one of those verdict labels
    # is present — a PR with no verdict label (a consumer that doesn't label, or a review
    # still in flight) is left untouched, never summoned merely for lacking the candidate
    # label.
    if: >-
      inputs.team_slug != '' &&
      github.event_name == 'workflow_run' &&
      github.event.workflow_run.event == 'pull_request' &&
      startsWith(github.event.workflow_run.head_branch, 'dependabot/')
    runs-on: ubuntu-latest
    steps:
      - name: Resolve PR number
        id: pr
        run: |
          set -euo pipefail
          NUM=$(jq -r '.workflow_run.pull_requests[0].number // empty' < "$GITHUB_EVENT_PATH")
          if [ -z "$NUM" ] || [ "$NUM" = "null" ]; then
            echo "No PR associated with this event; nothing to do."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "number=$NUM" >> "$GITHUB_OUTPUT"

      - name: Wait for the review to settle and classify
        if: steps.pr.outputs.skip != 'true'
        id: classify
        env:
          GH_TOKEN: ${{ secrets.ORG_TEAM_MEMBERS }}
          PR_NUMBER: ${{ steps.pr.outputs.number }}
        run: |
          set -euo pipefail

          read_state() {
            DATA=$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json author,state,labels)
            AUTHOR=$(jq -r '.author.login' <<<"$DATA")
            STATE=$(jq -r '.state' <<<"$DATA")
            LABELS=$(jq -c '[.labels[].name]' <<<"$DATA")
          }
          has() { jq -e --arg l "$1" 'index($l)' <<<"$LABELS" >/dev/null; }

          read_state
          if [ "$AUTHOR" != "app/dependabot" ]; then
            echo "Not a Dependabot PR (author=$AUTHOR); skipping."
            echo "escalate=false" >> "$GITHUB_OUTPUT"; exit 0
          fi
          if [ "$STATE" != "OPEN" ]; then
            echo "PR not open (state=$STATE); skipping."
            echo "escalate=false" >> "$GITHUB_OUTPUT"; exit 0
          fi

          # The review runs in parallel with CI and may not have applied its
          # verdict label when CI completes. Wait briefly for one of the three
          # verdict labels before deciding. Already-settled PRs (the common case —
          # the review finishes before CI) break out at once.
          for i in $(seq 1 12); do
            if has "claude-auto-merge-candidate" || has "needs-human-review" \
               || has "claude-review-unavailable"; then break; fi
            sleep 15
            read_state
          done

          if has "claude-auto-merge-candidate"; then
            echo "PR is an auto-merge candidate; the merge / ci-failure paths own it."
            echo "escalate=false" >> "$GITHUB_OUTPUT"; exit 0
          fi

          # Escalate ONLY on an explicit human-needed verdict from the caller's
          # review (`needs-human-review`, or `claude-review-unavailable` when the
          # review could not run). The mere absence of the candidate label is NOT
          # sufficient: a review still in flight, or a consumer that doesn't label
          # its Dependabot PRs, must not trigger a summons — that is the premature/
          # unwarranted summons this work removes. Such PRs are left untouched here
          # (not auto-merged, not escalated).
          if ! has "needs-human-review" && ! has "claude-review-unavailable"; then
            echo "No human-needed verdict label present; nothing to escalate."
            echo "escalate=false" >> "$GITHUB_OUTPUT"; exit 0
          fi

          # Pick the comment. `claude-review-unavailable` (the review could not run)
          # takes precedence over `needs-human-review` for wording.
          if has "claude-review-unavailable"; then
            echo "PR labelled claude-review-unavailable."
            COMMENT="⚠️ This Dependabot PR needs human review: the automated code review could not run, so it was not eligible for auto-merge. A reviewer has been assigned — please review the changes before merging."
          else
            echo "PR labelled needs-human-review."
            COMMENT="⚠️ This Dependabot PR needs human review: the automated code review did not clear it for auto-merge. A reviewer has been assigned — please review the Claude review comments and the changes before merging."
          fi
          echo "escalate=true" >> "$GITHUB_OUTPUT"
          { echo "comment<<EOF"; echo "$COMMENT"; echo "EOF"; } >> "$GITHUB_OUTPUT"

      - name: Escalate to human reviewer
        if: steps.classify.outputs.escalate == 'true'
        uses: macuject/.github/.github/actions/escalate-dependabot-pr@main
        with:
          pr_number: ${{ steps.pr.outputs.number }}
          repo: ${{ github.repository }}
          team_slug: ${{ inputs.team_slug }}
          org_token: ${{ secrets.ORG_TEAM_MEMBERS }}
          failed_sha: ${{ github.event.workflow_run.head_sha }}
          grace_seconds: ${{ inputs.autofix_grace_seconds }}
          dismiss_approval: "false"
          sentinel_label: needs-human-review-requested
          exclude_members: ${{ inputs.exclude_reviewers }}
          comment_body: ${{ steps.classify.outputs.comment }}
